{
	"name": "@kibhq/extension",
	"version": "0.6.0",
	"private": true,
	"description": "kib browser extension — save any webpage to your knowledge base",
	"scripts": {
		"build": "bun run build.ts",
		"dev": "bun run build.ts --watch"
	},
	"dependencies": {
		"@mozilla/readability": "^0.5.0",
		"turndown": "^7.2.0"
	},
	"devDependencies": {
		"@resvg/resvg-js": "^2.6.2",
		"@types/turndown": "^5.0.5"
	}
}
